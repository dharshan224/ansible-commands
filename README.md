# ansible-commands
## To Copy the FILE
- use the commands
  - present working directory
    ```
    ansible dev -a "pwd"
         /home/username
    ```
  - list
    ```
    ansible dev -a "ls"
    ```
  - list -a
    ```sh
    ansible dev -a "ls -a"
    ```
## To move the file
- 


