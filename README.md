# ansible-commands 
- use the commands
  - present working directory
    ```
    ansible dev -a "pwd"
    ``` 
  - list
    ```
    ansible dev -a "ls"
    ```
  - list -a
    ```sh
    ansible dev -a "ls -a"
    ```
    ## To Copy the FILE
## To move the file
- 


